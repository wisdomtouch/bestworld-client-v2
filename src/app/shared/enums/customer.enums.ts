export enum UserRole {
  Customer = "Customer",
}

export enum UserStatus {
  Pending = "0",
  Approve = "1",
  Decline = "2",
}

export enum KycStatus {
  pending = "Pending",
  reject = "Reject",
  approve = "Approve",
}
